import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { try { const count = await prisma.jobListing.count(); console.log(`Number of job listings: ${count}`); if (count > 0) { const sample = await prisma.jobListing.findFirst(); console.log("Sample record:", JSON.stringify(sample, null, 2)); } } catch (error) { console.error("Error:", error); } } main().then(async () => { await prisma.$disconnect(); }).catch(async (e) => { console.error(e); await prisma.$disconnect(); process.exit(1); });
